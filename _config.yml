# Site settings
title: gRally • changelog
description: 'Jekyll-Uno - a minimal, responsive theme for Jekyll'
url: 'http://docs.grally.net/'
baseurl: '/'
# google_analytics: 'UA-XXXXXX-X'
disqus_shortname: 'ghiboz'

author:
  name: 'gRally'
  #email: info@grally.net
  twitter_username: gRallySim
  facebook_username: gRallySim
  github_username:  gRallySim

defaults:
  -
    scope:
      path: ''
      type: 'posts'
    values:
        layout: 'post'

# Build settings
destination: _posts
paginate: 10
permalink: /:title/
markdown: kramdown
highlighter: rouge

kramdown:
  # use Github Flavored Markdown
  input: GFM
  # do not replace newlines by <br>s
  hard_wrap: false

gems: ['jekyll-paginate', 'jekyll-mentions', 'jekyll-sitemap', 'jemoji']
exclude: ['README.md', 'Gemfile', 'Gemfile.lock', 'screenshot.png']
